using UnityEngine;
using System.Collections;

public class RemindManagement : MonoBehaviour {
    [SerializeField]
    private GameObject m_remind;
	// Use this for initialization
	void Start () {
        //m_remind = GameObject.FindObjectOfType<RemindManager>().gameObject;
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
